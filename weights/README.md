# weights

Place trained project weights here:

```text
classification/weights/best.pt
segmentation/weights/best.pt
classification+segmentation/train/weights/best.pt
```

Model artifacts are ignored by Git. See `../docs/MODELS.md`.
